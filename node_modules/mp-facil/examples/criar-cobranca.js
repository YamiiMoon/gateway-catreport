import { MPFacil } from '../src/index.js';

const mp = new MPFacil({
  api_key: process.env.MP_ACCESS_TOKEN,
  id_app: process.env.MP_APP_ID,
  webhook: 'https://seusite.com/webhook'
});

async function main() {
  const cobranca = await mp.criarCobranca({
    produto: 'Curso de Node.js',
    id: 'curso-node-001',
    preco: 49.9
  });

  console.log(cobranca);
}

main();
