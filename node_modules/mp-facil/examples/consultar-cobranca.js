import { MPFacil } from '../src/index.js';

const mp = new MPFacil({
  apiKey: process.env.MP_ACCESS_TOKEN
});

async function main() {
  const pagamento = await mp.checkCharge(process.argv[2]);
  console.log(pagamento);
}

main();
